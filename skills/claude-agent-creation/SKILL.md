---
name: claude-agent-creation
description: Guides creation of Claude Code custom agents and their associated protocol files. Use when creating a new agent, standardizing an existing workflow into an agent, or converting a skill to an agent. Examples: "create a new agent", "make an agent for", "build a custom agent", "convert this skill to an agent".
---

Use `mcp__cognitive-memory__read_entity` to read and follow `protocols/claude-agent-creation`.
