---
name: context-mapping
description: Apply historical learnings and patterns when starting new tasks. Use when beginning implementation work, switching projects, or when user references past approaches. Examples: "what did we do last time?", "how did we handle this before?", "apply past learnings", "use the same approach as".
---

Use `mcp__cognitive-memory__read_entity` to read and follow `protocols/context-mapping`.
